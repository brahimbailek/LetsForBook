[phases.setup]
nixPkgs = ["nodejs-22_x"]

[phases.install]
cmds = [
  "npm install -g pnpm@9.1.0",
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "pnpm db:generate",
  "pnpm build --filter=web"
]

[start]
cmd = "chmod +x apps/web/start.sh && ./apps/web/start.sh"

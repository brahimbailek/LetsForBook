{
  "name": "@letsforbook/utils",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "clean": "rm -rf .turbo node_modules dist"
  },
  "dependencies": {
    "date-fns": "^3.3.1",
    "date-fns-tz": "^3.0.0"
  },
  "devDependencies": {
    "@letsforbook/config": "*",
    "typescript": "^5.3.3"
  }
}
